#cloud-config

repo_update: true
repo_upgrade: all
package_update: true
package_upgrade: true

ssh_pwauth: false

packages:
  - ca-certificates
  - curl
  - awscli
  - jq

runcmd:
  - sudo /tmp/add_kubeconfig.sh
